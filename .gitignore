# env / secrets
.env
.env.*

# data — never commit
data/
!data/.gitkeep

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# uv
# (uv.lock IS committed — do not ignore it)

# os
.DS_Store

# agent worktrees (other sessions' in-progress trees)
.claude/worktrees/
